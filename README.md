my-vault/
├── cmd/
│   └── vault/              # Основная точка входа
│       └── main.go         # Здесь запускается SSH-сервер или CLI
├── internal/               # Код, который нельзя импортировать извне (ядро)
│   ├── crypto/             # Шифрование и работа с ключами
│   │   ├── aes.go          # AES-GCM шифрование
│   │   ├── argon2.go       # Хеширование мастер-пароля
│   │   └── rand.go         # Генерация случайных паролей
│   ├── ssh/                # Настройка Wish (SSH-сервера)
│   │   └── server.go       # Конфигурация Wish и Middleware
│   ├── ui/                 # Интерфейс Bubble Tea
│   │   ├── model.go        # Главная модель (State)
│   │   ├── update.go       # Логика обработки событий
│   │   ├── view.go         # Рендеринг интерфейса
│   │   ├── components/     # Отдельные части UI (инпуты, списки)
│   │   └── styles/         # Стили Lip Gloss
│   └── storage/            # Работа с данными
│       ├── file.go         # Чтение/запись зашифрованного файла
│       ├── vault.go        # Структуры данных (Entry, Vault)
│       └── git.go          # (Опционально) Синхронизация через Git
├── pkg/                    # Общие хелперы (если нужно)
├── .ssh/                   # Ключи хоста для SSH (в .gitignore!)